diff --git a/quickjs-jit-helpers.h b/quickjs-jit-helpers.h
--- a/quickjs-jit-helpers.h
+++ b/quickjs-jit-helpers.h
@@ -121,5 +121,8 @@
     X(REGEXP, regexp, JS_JitHelperRegexp, MAP_OUT_TWO, 3, OWNED, CONSUMED,  \
       CONSUMED, NONE, OWNED,                                                 \
-      THROWING | ALLOCATING | REENTRANT | FINALIZING)
+      THROWING | ALLOCATING | REENTRANT | FINALIZING)                       \
+    X(ATOM_VALUE, atom_value, JS_JitHelperAtomValue, MAP_OUT_INDEX, 1,      \
+      OWNED, NONE, NONE, NONE, OWNED,                                       \
+      THROWING | ALLOCATING | REENTRANT)
 
 typedef enum JSJitHelperId {
diff --git a/quickjs-jit.h b/quickjs-jit.h
--- a/quickjs-jit.h
+++ b/quickjs-jit.h
@@ -17,7 +17,7 @@
 #endif
 
 #define QJSJIT_ABI_MAJOR 1u
-#define QJSJIT_ABI_MINOR 16u
+#define QJSJIT_ABI_MINOR 18u
 
 /* Frame flags are append-only. Stress collection is a test/debug contract:
  * every helper performs an allocating cycle-GC point before and after its
@@ -157,6 +157,7 @@ typedef struct JSJitConstantDescriptor {
     int32_t tag;
     uint32_t kind;
     uint32_t reserved;
+    uint64_t payload;
 } JSJitConstantDescriptor;
 
 typedef enum JSJitSnapshotStatus {
@@ -216,7 +217,7 @@
  * prefix and helper ID zero; every later helper is an append-only v1.1 tail.
  */
 #define QJSJIT_RUNTIME_API_MAJOR 1u
-#define QJSJIT_RUNTIME_API_MINOR 7u
+#define QJSJIT_RUNTIME_API_MINOR 8u
 
 #define QJSJIT_RUNTIME_FIELD_FRAME(field)                                   \
     JSJitHelperStatus (*field)(JSJitExecFrame *frame)
diff --git a/quickjs.c b/quickjs.c
--- a/quickjs.c
+++ b/quickjs.c
@@ -2834,6 +2834,13 @@ int JS_JitCaptureFunctionSnapshot(JSContext *ctx, JSValueConst function,
         constants[i].tag = JS_VALUE_GET_NORM_TAG(b->cpool[i]);
         constants[i].kind = qjsjit_constant_kind(ctx, b->cpool[i]);
         constants[i].reserved = 0;
+        constants[i].payload = 0;
+        if (constants[i].tag == JS_TAG_INT) {
+            constants[i].payload = (uint64_t)(int64_t)JS_VALUE_GET_INT(b->cpool[i]);
+        } else if (constants[i].tag == JS_TAG_FLOAT64) {
+            double value = JS_VALUE_GET_FLOAT64(b->cpool[i]);
+            memcpy(&constants[i].payload, &value, sizeof(value));
+        }
     }
 
     snapshot->function.struct_size = sizeof(snapshot->function);
@@ -19642,5 +19649,34 @@ JSJitHelperStatus JS_JitHelperRegexp(JSJitExecFrame *frame,
     return qjsjit_finish_output(frame, out);
 }
+
+JSJitHelperStatus JS_JitHelperAtomValue(JSJitExecFrame *frame,
+                                        uint32_t stack_map_id,
+                                        uint32_t output, uint32_t atom)
+{
+    JSFunctionBytecode *b;
+    JSStackFrame *sf;
+    JSValue *out, result;
+
+    if (qjsjit_validate_helper_frame(frame, true, stack_map_id, &b, &sf) < 0)
+        return JS_JIT_HELPER_EXCEPTION;
+    qjsjit_count_helper(frame, JS_JIT_HELPER_ATOM_VALUE);
+    if (!qjsjit_resolve_slot(frame, b, output, &out) ||
+        !qjsjit_valid_atom(frame->rt, (JSAtom)atom))
+        return qjsjit_reject_helper(frame, "invalid JIT atom value");
+    if (!JS_IsUndefined(*out))
+        return qjsjit_reject_helper(frame, "occupied JIT atom output");
+    if (qjsjit_stress_gc(frame) < 0)
+        return JS_JIT_HELPER_EXCEPTION;
+    result = JS_AtomToValue(frame->ctx, (JSAtom)atom);
+    if (JS_IsException(result)) {
+        *out = JS_UNDEFINED;
+        qjsjit_stress_gc_after_exception(frame);
+        return JS_JIT_HELPER_EXCEPTION;
+    }
+    *out = result;
+    return qjsjit_finish_output(frame, out);
+}
+
 #endif
 
 /* argv[] is modified if (flags & JS_CALL_FLAG_COPY_ARGV) = 0. */
